# NATS server configuration.
# Auth credentials are injected via environment variables by the container.

listen: 0.0.0.0:4222

http_port: 8222

jetstream {
    store_dir: /data/jetstream
    max_mem: 256MB
    max_file: 1GB
}

authorization {
    user: $NATS_USER
    password: $NATS_PASSWORD
}
