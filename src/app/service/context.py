"""The binding for this context: a thin shim whose `connect` binds the live transport
clients to the consistency model. It owns no domain type, holds no domain logic, and
makes no decision. A build overwrites this file. (docs/construct.md: binding)"""
