---
name: tca_forbidden_pattern_verb_absent_from_the_surface
description: The absent-verb anti-pattern. A modeled verb the class never defines. Read this while building a consistency model to see that the plan promised an act the code does not perform.
---

## Verb absent from the surface

A verb the ontology models that the class never defines: the row promised an act the code does not perform.

Every verb row renders a method on the consistency model. A modeled verb missing from the class is a transition the ontology authorized and the code never wrote; define the method the row names.

```python
# row names `settle`; the class has no settle method: define settle as the verb its row declares
```
