"""The inbound wire crossing: foreign models (another system's data shape, its keys
held as aliases, lifted whole into our meanings) and ordered unions (identity-free
foreign data that may fail or say no, the failure modeled as a value, never a caught
exception). A build overwrites this file. (docs/construct.md: foreign model, ordered
union)"""
