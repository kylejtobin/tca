"""Collections: domain sequences that are themselves a thing, a frozen
`RootModel[tuple[T, ...]]` with its own name, bound, or whole-sequence derivation. A
sequence with no meaning of its own is just a `tuple[T, ...]` field. A build overwrites
this file. (docs/construct.md: collection)"""
