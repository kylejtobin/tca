"""The atomic vocabulary: one semantic scalar per domain value, a frozen
`RootModel[P]` carrying its own constraint and referencing no other domain type.
Everything else composes these. A build overwrites this file.
(docs/construct.md: semantic scalar)"""
