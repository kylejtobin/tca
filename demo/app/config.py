"""Config: the only structure that reads the environment. A frozen `BaseSettings` of
declared scalars and secrets, constructed once by the composition root and injected.
A build overwrites this file. (docs/construct.md: config)"""
