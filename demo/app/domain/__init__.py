"""The domain: one package per bounded context. `context` is a placeholder name;
rename it for your domain (for example `orders` or `billing`), one such package per
context, each with its own frozen vocabulary and one consistency model."""
